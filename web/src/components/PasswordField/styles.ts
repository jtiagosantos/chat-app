import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  position: relative;

  svg {
    position: absolute;
    right: 12px;
    top: 8px;
    cursor: pointer;
  }
`;