import styled from 'styled-components';

export const Container = styled.main`
  width: 60%;
  height: calc(100vh - 64px);
  margin: 0 auto;
  padding: 2rem 0 1rem;

  position: relative;

  h2 {
    font-size: 1rem;
    color: #FFF;
    font-weight: normal;
  }
`;